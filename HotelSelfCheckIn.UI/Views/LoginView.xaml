<UserControl x:Class="HotelSelfCheckIn.UI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HotelSelfCheckIn.UI.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="400"
             Background="White">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <Image Source="/Assets/logo.png" 
                   Width="100" 
                   Height="100" 
                   Margin="0,0,0,20"/>
            <TextBlock Text="Username" Foreground="Gray" FontSize="12" Margin="0,10,0,0"></TextBlock> <!--> eticheta fixa <-->
            <TextBox Text="{Binding Username}" FontSize="16" BorderThickness="0,0,0,1" BorderBrush="LightGray" Margin="0,0,0,15"></TextBox> <!--> {Binding Username} ia textul si il trimite <-->
            <TextBlock Text="Password" Foreground="Gray" FontSize="12" Margin="0,10,0,0"></TextBlock>
            <PasswordBox FontSize="16" BorderThickness="0,0,0,1" BorderBrush="LightGray" Margin="0,0,0,25"></PasswordBox>
            <Button Content="LOG IN" Background="Black" Foreground="White" Height="40" FontWeight="Bold" Command="{Binding LoginCommand}">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"></Setter>
                    </Style>
                </Button.Resources>
            </Button>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" Margin="0,10,0,0"></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
