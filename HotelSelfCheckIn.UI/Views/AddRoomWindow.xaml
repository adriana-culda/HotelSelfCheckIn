<Window x:Class="HotelSelfCheckIn.UI.Views.AddRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Adauga Camera Noua" Height="350" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    
    <!-->Partea de completare<-->
    <StackPanel Margin="20">
        
        <TextBlock Text="Numar Camera:" 
                   Margin="0,0,0,5" 
                   FontWeight="Bold">
        </TextBlock>
        
        <TextBox Text="{Binding NewRoom.RoomNumber, UpdateSourceTrigger=PropertyChanged}" 
                 Height="30"
                 Margin="0,0,0,15"
                 Padding="5">
        </TextBox>

        <TextBlock Text="Tip Camera (Single/Double/Triple/Family):" 
                   Margin="0,0,0,5" 
                   FontWeight="Bold">
        </TextBlock>
        
        <TextBox Text="{Binding NewRoom.Type, UpdateSourceTrigger=PropertyChanged}" 
                 Height="30" 
                 Margin="0,0,0,15"
                 Padding="5">
        </TextBox>
        
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right">
            
            <Button Content="Anuleaza" 
                    IsCancel="True"
                    Width="100"
                    Height="30" 
                    Margin="0,0,10,0">
            </Button>
            
            <Button Content="Salveaza" 
                    Command="{Binding SaveRoomCommand}" 
                    Width="100" Height="30" 
                    Background="#00B4D8"
                    Foreground="White"
                    FontWeight="Bold">
            </Button>
        </StackPanel>
    </StackPanel>
</Window>