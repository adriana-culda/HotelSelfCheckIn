<UserControl x:Class="HotelSelfCheckIn.UI.Views.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="540" d:DesignWidth="780">

    <Grid Background="#F5F7FA">
        <Border Background="White" CornerRadius="12" Margin="25" Padding="25">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="1" Opacity="0.05"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Configurarea Regulilor Generale" FontSize="20" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,15"/>

                <UniformGrid Grid.Row="1" Columns="2" Margin="0,0,0,15">
                    <StackPanel Margin="0,0,10,0">
                        <Label Content="Oră Check-in (ex: 14:00):" Padding="0,5"/>
                        <TextBox Text="{Binding CheckInTime}" Height="32" VerticalContentAlignment="Center" Padding="5,0"/>
                    </StackPanel>
                    <StackPanel Margin="10,0,0,0">
                        <Label Content="Oră Check-out (ex: 11:00):" Padding="0,5"/>
                        <TextBox Text="{Binding CheckOutTime}" Height="32" VerticalContentAlignment="Center" Padding="5,0"/>
                    </StackPanel>
                </UniformGrid>

                <TextBlock Grid.Row="2" Text="Alte limitări și reguli" FontSize="18" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,10"/>

                <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,15">
                    <Label Content="Durată minimă rezervare (zile):" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding MinDays}" Width="60" Height="32" Margin="10,0,0,0" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                </StackPanel>

                <DockPanel Grid.Row="4" Margin="0,5,0,15">
                    <Label DockPanel.Dock="Top" Content="Reguli generale hotel:"/>
                    <TextBox Text="{Binding GeneralRules}" 
                             AcceptsReturn="True" 
                             TextWrapping="Wrap" 
                             VerticalAlignment="Stretch"
                             Padding="10"/> 
                </DockPanel>

                <Button Grid.Row="5" Content="Salvează Configurarea" Width="180" Height="38" Background="#00B4D8" Foreground="White" FontWeight="Bold" BorderThickness="0" HorizontalAlignment="Right" Cursor="Hand"/>
            </Grid>
        </Border>
        
    </Grid>
</UserControl>