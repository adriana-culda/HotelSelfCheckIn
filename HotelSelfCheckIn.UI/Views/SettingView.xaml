<UserControl x:Class="HotelSelfCheckIn.UI.Views.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions> 
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="Black"> 
            <StackPanel Margin="0,20">
                <Image Source="/Assets/logo.png" Width="60" Height="60" Margin="0,0,0,10" Stretch="Uniform"/>
                <TextBlock Text="Hotel Management System" Foreground="White" FontSize="14" FontWeight="Bold" TextAlignment="Center" Margin="10,0,10,30" TextWrapping="Wrap"/>
                
                <Button Content="Dashboard" Margin="0,5" Height="40" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalAlignment="Left" Padding="20,0,0,0"/>
                <Button Content="Client" Margin="0,5" Height="40" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalAlignment="Left" Padding="20,0,0,0"/>
                <Button Content="Room" Margin="0,5" Height="40" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalAlignment="Left" Padding="20,0,0,0"/>
                <Button Content="Reservation" Margin="0,5" Height="40" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalAlignment="Left" Padding="20,0,0,0"/>
                
                <Button Content="Setting" Margin="0,5" Height="40" Background="#3E3E42" Foreground="White" BorderThickness="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="20,0,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#00B4D8">
                <DockPanel Margin="20,0">
                    <TextBlock Text="{Binding CurrentDateTime}" VerticalAlignment="Center" Foreground="White" FontSize="13"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="{Binding AdminName, StringFormat='Welcome: {0}'}" Foreground="White" Margin="0,0,20,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        <Button Content="Log Out" Foreground="White" Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                    </StackPanel>
                </DockPanel>
            </Border>
            
            <Grid Grid.Row="1" Background="#F5F7FA">
    <Border Background="White" CornerRadius="12" Margin="25" Padding="25">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="1" Opacity="0.05"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Configurarea Regulilor Generale" FontSize="20" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,15"/>

            <UniformGrid Grid.Row="1" Columns="2" Margin="0,0,0,15">
                <StackPanel Margin="0,0,10,0">
                    <Label Content="Oră Check-in (ex: 14:00):" Padding="0,5"/>
                    <TextBox Text="{Binding CheckInTime}" Height="32" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="10,0,0,0">
                    <Label Content="Oră Check-out (ex: 11:00):" Padding="0,5"/>
                    <TextBox Text="{Binding CheckOutTime}" Height="32" VerticalContentAlignment="Center"/>
                </StackPanel>
            </UniformGrid>

            <TextBlock Grid.Row="2" Text="Alte limitări și reguli" FontSize="20" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,15"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,10">
                <Label Content="Durată minimă rezervare (zile):" VerticalAlignment="Center"/>
                <TextBox Text="{Binding MinDays}" Width="60" Height="32" Margin="10,0,0,0" VerticalContentAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Row="4" Margin="0,5,0,15">
                <Label Content="Reguli generale hotel:"/>
                <TextBox Text="{Binding GeneralRules}" 
                         AcceptsReturn="True" 
                         TextWrapping="Wrap" 
                         VerticalAlignment="Stretch"
                         MinHeight="100"
                         Height="Auto"/> 
            </StackPanel>

            <Button Grid.Row="5" Content="Salvează Configurarea" Width="180" Height="38" Background="#00B4D8" Foreground="White" FontWeight="Bold" BorderThickness="0" HorizontalAlignment="Right"/>
        </Grid>
    </Border>
</Grid>
        </Grid>
    </Grid>
</UserControl>